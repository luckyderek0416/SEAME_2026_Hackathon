# 인수인계 — 2026-07-13 새벽 ★최신

run49~73 상세는 **[RUN_LOG_2026-07-12.md](RUN_LOG_2026-07-12.md)** (하단 두 섹션).

## 0. 한 줄 요약

**★★ run73 = 최초의 완전 자율 풀 미션 완주** — 출발→분기→RA→링→게이트(군집
카운트)→탈출→흰 병합→흰 코스→FINISH 인식→자가 정지, 전 구간 무개입 (영상 확인).
다음 최우선 = **재현 런**으로 안정성 확인.

## 1. 현재 배포 스택 (보드=로컬 md5 일치, HEAD = db0a346)

- 베이스: run60 시점 코드(e6cdcbb) + **게이트만 군집 카운트 v2 로 교체**
- perception: SW 코리도(진입 440f/탈출 400f, 라이브), 탈출 개방구간, 흰 인계 창,
  aruco IndexError 수정 — 전부 run60 검증분
- decision 게이트 (군집 v2, RUN_LOG 스펙 참조): gates=2, blank 7.0(무스케일),
  cluster gap 1.8s / ON 0.18s, yaw 위생하한 1.0. **오프라인 검산 9/9 + run73 실전 1/1**
- 스로틀: drive 0.19 / yellow_drive·slow 0.165 / 킥 0.23. **동적 보정은 코드에
  있으나 라이브 gain 0** (aux.sh) — 팩 비선형(흰 과속+노랑 저속 동시) 때문.

**라이브 프로토콜 (aux.sh 자동)**: gate_blank_s 7.0, throttle_adapt_gain 0.0,
follow_yellow_ratio 0.02, exit_yellow_frac 0.75, sw_entry 440, sw_exit 400,
sw_exit_straight_k 999.

## 2. 하드웨어 상태

- **배터리 팩 말기**: 무부하 7.7~8.0V 대역, 부하 새그 ~1V (min 6.98V 관측).
  스로틀 대역별 비선형 — 낮은 틱(323 이하)은 정지 위험(run63/70/71), 흰 구간은
  과속. **틱 사다리 주의: 1틱=0.0098, 0.16≡0.165(틱324), 0.17≡0.174(325)**.
  충전 후엔 과속 쪽 재캘리 감안. 교체 팩이 정답.
- 보드 급전원 상실 이력 2회(run55/58) + Wi-Fi 동글 사망 반복 — **원격 정지 불가
  시 즉시 ESC 물리 컷**. 재부팅 시 /tmp 스크립트 재배포 필수 (레포 scratchpad 에
  사본; launch 로그는 ~/auto_race.log 로 생존).
- IP 유동: 172.20.10.5↔6 (재부팅 시 서브넷 스캔으로 재확인, ~/.ssh/config 갱신)

## 3. 남은 문제 (우선순위)

1. **run73 재현 확인** — 성공 1회는 우연일 수 있음. 같은 구성 2~3런.
2. 배터리 충전/교체 후 밴드별 재검증 (특히 만충 고속 밴드에서 군집 게이트·스로틀).
3. V-피드포워드 (전압→스로틀): 캘리 산점 축적됨 (250cm 출발 실측 + 링 9129.72mm),
   battery_node 활성화 필요 (launch 주석의 printk 폭주 이력 가드 확인 후).
4. 오발 복구 확장: 군집 게이트는 원샷 소진(run72) 대신 재기회 구조로 확장 여지.
5. FINISH 판정 검증: run73 은 성공했으나 run69/72 에서 오판 이력 (빨간 요소 오인).

## 4. 운영 절차

- "멈춰" = stop.sh → 0x40/0x42 OK + 프로세스 0 확인 → 분석. i2c errno=16 이면
  ESC 컷 요청. 동글 죽어 ssh 안 되면 **ESC 물리 컷이 유일한 정지 수단**.
- 발화 판정 읽는 법: auto_race.log 의 `gate=2 exitV=0` = 군집 주 경로(정답),
  `exitV=2` = 표결 경로, `exitV=1` = 타임아웃 (링에서 길 잃음).
- 250cm(출발→래치) / 9129.72mm(링 랩) 으로 속력 사후 산출 가능.
- 오프라인 검산: 게이트 로직 변경 시 scratchpad 의 9런 시뮬 스크립트로 회귀 확인.

## 5. git

- luckyderek 푸시 완료 (~e8b0df2) + 이후 커밋: db0a346(군집 v2), RUN_LOG 문서.
- 롤백 지점: e8b0df2 (run60 구성) / db0a346 (run73 구성 = 현재).
