# 인수인계 — 2026-07-13 저녁 ★최신

run49~80 상세는 **[RUN_LOG_2026-07-12.md](RUN_LOG_2026-07-12.md)** (하단 세 섹션).

## 0. 한 줄 요약

**★★ run80 = 무개입 풀 미션 체인 완주** — 체커 출발→분기→링 등판→A 재도달
발화·우측 탈출→병합→ArUco 마커 정지·제거 후 재출발→빨간불 FINISH→자가 정지.
게이트는 run76~78+80 4회 재현. 다음 = 충전/새 팩으로 재현 확인.

## 1. 코스 (사용자 확정 — 지도 오독 주의!)

- **A = 링 위쪽 입구(진짜 정지선) — 진입도 탈출도 A.** B = 우하단 입구(통과만).
- 체커→하단 분기→3142 길→B 통과→우측면 등판→A(RA 시작)→한 바퀴→**A 재도달
  발화→우측 탈출**→상단→왼쪽 직선(ArUco)→체커 빨간불 끝. 오른쪽 S커브 무시.
- 연습 트랙엔 빨간 노면 없음 → ArUco 는 "면적≥0.02 접근 정지" 경로만 검증됨.
  대회장에선 in_red_zone 즉시정지·감속 경로 살아남 — **당일 rr≥0.05 정적 확인**.

## 2. 현재 배포 스택 (보드=로컬 3-way md5 일치)

- 게이트: 군집 카운트 v2 + 표결 자기군집 가드(84b1e95) + **SW 교차 게이트 상시**
  (crossline_sw_gate 1 — B 개구부 페인트 기각, run75 이탈 유형 봉합)
- 소스 기본값 확정: gate_cluster_on_s **0.25** / marker_clear_frames **8** /
  sw_entry_frames **1200**(코리도 60s, run79 개구부 이탈 봉합) / aruco_dict
  **DICT_6X6_50**(실물 마커 6×6 id3 — 4X4 였으면 원리적 무검출!)
- 스로틀: run60 값 그대로 (drive 0.19 / yellow·slow 0.165 / 킥 0.23, adapt gain 0)
- aux.sh(라이브 주입)는 위와 동일 값 — 이중 안전. /tmp 는 재부팅마다 재배포
  (사본: 레포 세션 scratchpad, launch.sh 는 heredoc 재생성 — aruco_dict 인자 포함!)

## 3. ArUco / FINISH 미션 로직

- DRIVE + 마커 검출 + (빨간존 내 OR 면적≥marker_area_trigger 0.02) → OBSTACLE_STOP
- 마커 소실 marker_clear_frames(8f≈0.8s) 연속 → 재출발 (run80 실증: 정지 1회, 채터링 없음)
- obstacle_done OR race_t≥60s → 빨간불 무장; red_light 3프레임(conf 0.5) → FINISH→DONE
- 주의: 마커 사전 불일치가 3런을 태웠다. **새 마커 인쇄 시 반드시 사전/ID 확인**
  (tools/identify_aruco.py 또는 cv2 전 사전 스캔).

## 4. 하드웨어 (긴급)

- **배터리 팩 사망 직전**: run80 부하 최저 **6.39V**. 랩타임 15.5→29s 로 늘어짐
  (모든 시간 가정 왜곡의 근원). **충전/교체 없이 추가 런 금지 권장** — 브라운아웃
  이력 2회(run55/58) 있음.
- 보드 재부팅/동글 사망 반복: ssh 안 되면 **ESC 물리 컷이 유일한 정지 수단**.
  IP 유동 172.20.10.5↔6 (서브넷 1~14 스캔).

## 5. 운영 절차

- "멈춰" = stop.sh → 0x40/0x42 OK + 노드 0 + 레코더 kill 확인 후에만 분석.
- 발화 로그: `gate=1 exitV=2` = 표결 경로(#1 침묵 후 A 재도달 첫 프레임 발화 — 현재
  정상 패턴), `exitV=0` = 카운트 경로, `exitV=1` = 타임아웃.
- 군집 앵커: B 개구부 = circle 의 65~74%, A 재도달 = ~100% (랩 15~29s 배터리 의존).
- 게이트 로직 변경 시 scratchpad 11런 시뮬 + sw_replay.py 리플레이로 회귀 확인.

## 6. git

- 이번 커밋 = run80 박제 (게이트/SW게이트/코리도/ArUco 사전/문서). 푸시: luckyderek.
- 롤백 지점: e8b0df2(run60) / db0a346(run73) / **이번 커밋(run80, 권장)**.
