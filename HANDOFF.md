# 인수인계 — 2026-07-12 오전 (밤샘 세션 → 주간 교대) ★최신

run28~48 (21런). 런별 상세는 **[RUN_LOG_2026-07-12.md](RUN_LOG_2026-07-12.md)** 참조 —
여기는 교대자가 당장 알아야 할 것만.

## 0. 한 줄 요약

**★ run47 = 무개입 전체 체인 완주** (출발→분기→RA→링 한 바퀴→게이트 탈출→탈출로),
그것도 죽어가는 7.8V 팩으로. 소프트웨어 스택은 사실상 완성 단계 —
**최우선 과제는 코드가 아니라 배터리 교체**다.

## 1. 현재 배포 스택 (보드=로컬 md5 일치, 리빌드 완료)

| 수리 | 내용 | 검증 |
|---|---|---|
| 카메라 피치 재캘리 (물리) | BEV 평행성 폐루프로 교정 — 밤새 전 구간 좌편향의 기저 원인이었음. **마운트 건드리면 재교정 필수** (아래 §4 절차) | run42~48 |
| P85 1L | 분기 1L의 선 추정을 전체평균→최우측 클러스터(x 85퍼센타일), 하단 2밴드만 사용 | 진입 성공 42/43/46/47/48 |
| fk 소실 규칙 | RA+5s 후 갈림길 서명(fork) 프레임 = 소실 취급 → 개구부 쐐기 추종 원천 차단 | run42~48 |
| 게이트 3.6 | yaw_gate_min 4.2→3.6 (진짜 실측 3.61~4.42 / 가짜 최대 3.31) | **5연속 발화** (42/43/46/47/48) |
| DRIVE 조향-유지 | 소실 시 직진 대신 직전 조향 EMA 유지 (3s 감쇠) — 전환부 직진 관통 방지 | run48 |
| 노란 구간 0.165 | yellow_drive_throttle 소스 고정 | run47/48 |

**라이브 프로토콜 (매 런치 후, 소스 승격 금지)**:
`ra_blind_bias -0.18` (저전압 서보 보정 — **새 팩이면 생략**, 소스 기본 -0.15가 정답),
`gate_blank_s 18.5` (승격 후보).

## 1.5 신규 (07-12 오전): SW 코리도 추적 — 구현·오프라인 검증 완료, 기본 OFF

RA 진입/탈출 갈림길의 하드코딩 호(entry/exit_steer_bias, ra_blind_bias)는 시간 기반
개루프라 배터리별 속도 차로 런마다 다른 호를 그린다 (run47 통과/run48 실패가 같은 코드).
대체: **전이 창 동안 슬라이딩 윈도우+2차 피팅으로 기대 곡률 방향의 선을 물어 위치 기반
폐루프로 승격**. 유효 락 프레임만 밴드를 대체, 실패 프레임은 기존(run47) 경로 그대로 =
바이어스/블라인드 호는 삭제가 아니라 폴백으로 강등. **창 밖 전 구간 무변경.**

- 파일: lane_detector.py(`_sw_corridor_bands` + process 창 블록), lane_node.py(sw_* 파라미터)
- **기본 OFF (sw_entry_frames=0, sw_exit_frames=0) — 배포해도 run47 거동과 비트 동일**
  (오프라인 리플레이 576프레임 비트 동일 확인). 켜기 (라이브, 매 런치 후):
  ```bash
  ros2 param set /lane_node sw_entry_frames 140   # RA 진입 창 ~7s (링 초반 확률구간 커버)
  ros2 param set /lane_node sw_exit_frames 60     # RA 탈출 창 ~3s
  # 끄기 = 둘 다 0 → 창 도중에도 즉시 해제(킬스위치 내장), run47 복귀
  ```
- 오프라인 검증 (run42 dbg 프레임 리플레이, scratchpad `sw_replay.py`):
  진입 창 락률 64% (락 = 코리도 우측 경계 정확 추적), 방향 게이트 음성 테스트
  오락 1/16 (반대 방향 선 거부), 미러 탈출 창 92% + 점선/실선 페어(nl=2) 동작.
- 멀티에이전트 적대 리뷰 22건 발견 → 확정 결함 전부 수정 (라이브 0 설정 크래시 가드
  2건, 단선 좌/우 분류를 `_combine_lr` 우선순위와 일치화, 라이브 킬스위치, 디버그 고착).
- **미검증 (다음 조 몫)**: 실차 20Hz A/B (리플레이는 4Hz dbg 기반 한계), 진짜 탈출
  코리도 (f42 탈출 프레임은 개입 후 구간이라 미러 검증만 됨). frame_rec 켜고 굴리면
  dbg 에 `[SW+]`(락)/`[SW]`(폴백) 태그 + 주황 피팅 곡선으로 프레임 단위 판정 가능.
- **보드 미배포**: 보드 오프라인이라 로컬 소스만 갱신. 배포 시 scp + `colcon build
  --packages-select perception` + md5 대조 필수.

## 2. 하드웨어 상태 (심각)

- **배터리 팩 수명 종료 중**: 무부하 8.29→7.6~7.8V, 충전해도 못 잡아둠. 쉬면 회복(빨라짐)/
  돌리면 처짐(느려짐) → 런별 속도 널뜀 → "빠르면 얕게, 느리면 깊게" 양방향 회전 실패.
  **교체 팩이 모든 것의 전제.** 검증 런은 "연달아 굴리기(처진 밴드 고정) + 긴 휴식 후
  첫 런은 버리는 런" 프로토콜로.
- **보드 브라운아웃 리부팅** (주행 중 3회+): 부하 스파이크에 보드 전원 레일이 꺼짐.
  전원 분리 불가 확인됨. **징후 = 카메라(노드) 죽고 차는 계속 감 → 즉시 ESC OFF**
  (PWM 칩이 마지막 명령 유지 — 소프트웨어로 못 세움). 리부팅 후엔 /tmp 스크립트 재배포 필수.
- 동글 사망 반복 (`device was removed`) → 뽑고 3s 재삽입, 안 되면 reboot. i2c 잠김도
  간헐 재발 (정지 시 0x40 errno=16 이면 재시도, 빈 주소까지 16이면 전원 완전 차단 10s).
- ESC 아밍: 리부팅 폭주 후엔 비중립 PWM 이 남아 ESC 가 아밍 거부 — stop.sh 로 중립
  확인 후 ESC OFF→ON.

## 3. 남은 문제 (우선순위)

1. **링 초반 확률 구간 (RA+2~7s)** — 개구부 꼬리, 양쪽 선이 다 안 보이는 구조적 소실
   구간 + 저전압 언더스티어. run47 통과 / run48 이탈(1개입). **새 배터리에서 재평가 먼저**
   (run28 은 그냥 통과했던 구간).
2. 탈출 후 노랑→흰 전환부 흰 재추종 불안정 (run48 — 직진 관통은 조향-유지가 막음).
3. 게이트 yaw 여유 얇음 (run48 에서 3.61 vs 3.6, 여유 0.01). 재배치 후보 3.45 (미결).
4. 배경: FINISH 빨간불 경로 미검증, aruco IndexError, battery_node off, git 미커밋
   (아래 §5).

## 4. 운영 절차 (이 세션에서 확립)

- **"멈춰" 프로토콜**: stop.sh → 0x40/0x42 OK 확인 (errno=16 이면 "ESC 꺼주세요" 경고
  + 재시도) → 프로세스 0 확인 → 그 다음에 분석. pkill 은 브래킷 패턴
  (`pgrep -f 'telemetry_dua[l]'`) — 자기 세션 킬 함정 방지.
- **카메라 피치 검사/교정**: 차를 출발선 정위치에 → 스택 잠깐 올려 dbg 프레임 캡처 →
  BEV 의 두 흰 기둥이 평행하면 OK, 위로 벌어지면 렌즈를 살짝 위로(반대면 아래로) →
  재캡처 반복. 5분.
- 온보드 디버그 녹화: `/tmp/frame_rec.py` (raw 10Hz + dbg 5Hz → ~/frames). 원인 불명
  이탈은 텔레메트리보다 dbg 프레임(마스크+중심 오버레이)이 훨씬 빠르게 답을 준다 —
  이번 세션의 3대 발견(원거리 호 오염, 누운 실선, BEV 왜곡)이 전부 여기서 나옴.
- 텔레메트리 아카이브: `~/SEAME_telemetry/2026-07-12/` (run28~48).

## 5. 미커밋 변경 (커밋 대기)

perception(lane_detector/lane_node — P85·2밴드·fk규칙·전이창off), decision(decision_node/
state_machine — 3.6·0.165·조향유지·reacq 등), HANDOFF.md, RUN_LOG_2026-07-12.md.
사용자 승인 후 luckyderek 브랜치에 커밋/푸시할 것.

---

# 인수인계 — 2026-07-11 (야간 교대용)

주간 세션 후반부: run17~27 + RA 조향 체계 재설계.
(그 아래 "오후" 섹션은 run7~16, 파라미터 값 다수가 이후 변경됨.)

## A. 런 타임라인 (run17~27)

```
run17  1L 창 2초가 저속에서 부족 → entry_oneline_frames 40→80 (4초)
run18  자율 아님 — 배터리 부족으로 전 구간 손으로 밀었음. 이 런의 타이밍/yaw 는
       밀기 속도 기준이라 캘리브레이션에 쓰지 말 것
run19  스로틀 전체 +0.01 (배터리 내부저항 손상 보상) → 자주행 복귀. 이후 "원상복구"
       기준점 = run19 코드
run20  ★ 사상 첫 게이트 탈출 (RA+24.8s, yaw 4.12 = 진짜 탈출선 실측). 블라인드 0%
       완주 → 링 순환은 순수 PID 로 충분함이 입증됨
run21  RA 중 차선 소실 → 19초 직진 이탈 → ra_blind_bias -0.15 신설 (소실 프레임 한정)
run22  merge 가산 바이어스(+0.15)가 오염된 피팅을 쫓는 PID(-0.39)에 압도돼 붕괴
       → 하드 오버라이드(조향 고정)로 전환
run23  하드 0.41 로도 바깥 밀림 (육안) → merge_steer_bias -0.18 (고정 0.44)
run24  합류부(image0 파란 지점) 직진 이탈 재현
run25  yaw 4.28 게이트 탈출 성공. 단 ① 단선=우측 규칙이 RA 전체 적용(과범위) →
       /decision/merge_zone Bool 플럼빙 + "0→1 재획득 시만" 으로 축소,
       ② 탈출 직후 급오버스티어 탈선 → 탈출 락(+0.18/2.5s)을 소실 프레임 한정으로
run26  창 안 nl 2→1 오염: 연속성 추적이 남은 우측선을 좌측으로 오인 → off +1.00
       → 창 종료 시 급우회전 → 30s 페일세이프
—      yaw 창의 구조적 결함 확정: yaw = 조향편차×시간 ⇒ 같은 스로틀이라도 런별
       속도 차로 창 위치가 밀림 (진짜 탈출선 yaw 실측 4.12~4.28 로 출렁)
07-12  ★ RA 조향 재설계 배포: yaw 위치창 [2.45,3.6] + 하드 오버라이드 폐기 →
       이벤트 기반 "RA+5s 이후 nl 0→1 재획득 = 우측 경계" (reacq_arm_s=5.0).
       실측 근거(런19~26 RA 에피소드 8개 분리 분석): 5s 이후 0→1 은 합류부
       (RA+22.5~27.8s)에서만 발생·오발 0건, 2→1 은 순항 중 일상적(3~18회/랩)이라 제외
run27  재설계 검증 실패 — 단, merge 도달 전에 죽어서 재설계 자체는 미검증.
       진입 개구부에서 nl=2 중점(두 갈림길 선의 가운데) 오인 → 직진 이탈 →
       26.6초 블라인드 도넛. run26 도 같은 웨지(RA+1.9~2.2s, off +0.12,
       steer 0.07)가 있었고 0.4초 만에 운좋게 회복했던 것 — 코드 차이 아님
```

## B. ★ 미결 제안 (사용자 승인 대기 — 야간에 이어서 논의)

**run27 실패 모드의 수정안** (설계 설명까지 끝났고 승인만 남음):
- 진입 락 해제를 **3초 타이머 전용**으로 (현재는 fork 재수렴으로 1.77초에 조기 해제
  — run25/26/27 전부 동일, 웨지(1.9s~)가 시작되기 직전에 락이 죽는 구조)
- 락 동안 **조향 하한 0.41** (= 0.26 + 0.15, 링 실측 요구 곡률). 가산 바이어스만으론
  부족 (최악 틱: PID -0.23 + 0.15 = 0.18, 여전히 우회전). 하한 0.26(직진 허용)도
  부족 — 직진이 바로 이번 실패 모드. PID 가 더 좌로(0.5~0.63, 진입 훅) 꺾는 건 허용.
- 리스크: 3초간 우측 교정 불가. 단 하한이 링 곡률이라 안쪽 침투는 구조적으로 어렵고,
  run19~27 진입 실패는 전부 바깥/직진 이탈이었음.

## C. 현재 배포 상태 (보드=로컬 md5 일치, decision 리빌드 완료)

| 항목 | 값 | 비고 |
|---|---|---|
| 스로틀 | kick 0.23 / drive 0.19 / yellow 0.17 / slow 0.165 | 배터리 손상 보상 +0.01 포함 |
| entry_steer_bias | **-0.15** / entry_lock_release_s 3.0 | 실제 해제는 1.77s (fork 재수렴) — §B 참조 |
| circle_steer_bias | 0.0 | 링은 순수 PID (run20 입증) |
| ra_blind_bias | -0.15 | 소실 프레임 한정, 진입 락 중 제외. **구 merge 창 예외는 삭제됨** |
| reacq_arm_s | **5.0 (신규)** | RA+5s 후 /decision/merge_zone=True → perception 0→1 재획득=우측 규칙 무장 |
| merge_steer_bias / merge_yaw_lo/hi | 선언만, **미사용** | 07-12 폐기. 이력 보존용 |
| exit_steer_bias | +0.18 / 2.5s, 소실 프레임 한정 | **성공 탈출에서 미검증** |
| 게이트 | blank 17 / min_loop 17 / yaw_gate_min 4.2 / yaw_lap 9.0 / max_loop 30 | 불변. 단 4.2 는 진짜 쪽 여유 2%뿐 (아래 D) |
| perception | entry_oneline_frames 80, center_jump_max_ratio 0.10 (nl=2 스킵), 0→1 재획득=우측 규칙 | run27 에서 소스 변경 없음, 리빌드 불필요였음 |
| fork_bias | 조향 기여 제거 (방안1) | turn_latch 는 텔레메트리 마커로만 |

## D. 이 구간의 교훈 (야간에 특히 유효)

- **jsonl 아카이브 오염 주의**: 좀비 텔레메트리 8개가 run19~25 파일에 뒤 런들을 이어붙임.
  이 파일들 분석 시 STATE 이벤트로 RA 에피소드를 분리해서 볼 것. run27.jsonl 부터 클린
  (시작 전 좀비 정리 + 정지 시 스트리머 kill 습관화).
- **ssh 비대화형엔 ROS 미소싱** → telemetry 실행은 반드시
  `source /opt/ros/humble/setup.bash && source .../install/setup.bash &&` 프리픽스.
- **진짜 탈출선 yaw = 4.12~4.28 실측** (가짜 실선 군집 최대 3.11). yaw_gate_min 4.2 는
  가짜 쪽 여유 26% / 진짜 쪽 겨우 2% — 속도가 조금만 빨라지면 진짜 선을 막아 페일세이프행.
  재배치 후보 3.6 (가짜/진짜 중점, 양쪽 ±12%). 파라미터 1개라 라이브 실험 가능.
- **위치 기반(yaw 창) → 이벤트 기반 전환이 이번 재설계의 축**: yaw 는 시간의 사본이라
  속도에 취약. 시간 하한(5s)만 쓰는 이벤트 트리거는 속도에 사실상 면역.
- 배터리 8.26V (run27 직전) — 양호. INA219: bus3 addr 0x42 reg 0x02, v=(raw>>3)×0.004
  (바이트 스왑 필요).
- **텔레메트리 아카이브 위치**: `~/SEAME_telemetry/2026-07-11/run2~27.jsonl` (노트북,
  리포 밖 — 세션 스크래치패드는 재부팅 시 소실이라 여기로 백업함). run27 만 단일 런
  클린 파일, run19~25 는 뒤 런들이 이어붙어 있으니 STATE 이벤트로 에피소드 분리 필수.

## E. 미션 체인 상태 (주간 최종)

```
초록불 → 흰 추종          ○ 안정
분기(흰→노랑) 1L 진입      ○ 12+ 연속
노란 접근로 → RA 게이트    ○ 안정 (7.5s 가드)
진입 락 + 개구부 통과      △ run27 실패 모드 노출 — §B 수정안 승인 대기 ★야간 1순위
링 순환 (순수 PID)        ○ 15~21s 안정, 블라인드 0% 사례 있음 (run20)
합류부 통과               △ 이벤트 기반 재설계 배포됨, 미검증 (run27 이 그 전에 죽음)
게이트 탈출               ○ 2회 성공 (run20 yaw4.12, run25 yaw4.28)
탈출 후 차선 복귀          △ 조건부 탈출 락 배포됨, 미검증
FINISH (빨간불 정지)      미도달
```

## F. 야간 할 일 (우선순위)

1. §B 진입 웨지 가드 — 사용자 승인 받고 배포 (state_machine ROUNDABOUT 블록 진입 락 부분)
2. 검증 런: 진입 통과 → 새 이벤트 기반 합류부 → 게이트 탈출 → 조건부 탈출 락 (한 런에 4개 검증 가능)
3. yaw_gate_min 4.2→3.6 재배치 논의 (사용자에게 제안 완료 상태, 미결)
4. debugging-manual (1).md 가 run19 무렵 기준으로 낡음 — 갱신은 사용자 허락 필요
5. 배경: aruco_node OpenCV5 IndexError, FINISH 경로 미검증, battery_node(use_battery) 복원, git 미커밋 (decision/perception/README + INA219 폴더)

---

# 인수인계 — 2026-07-11 (오후, 주간 세션)

아침 섹션 이후 주간 자율 런 10회(run7~16)의 변경분.

## 0. 오늘 오후의 돌파구 (시간순)

```
run7~8   분기(흰→노랑) 진입 실패 재현 + 문턱 실험 (0.03↔0.01, 죽은 레버 판명)
run9     실패 원인 확정: nl=2 중점이 조향을 분기 쐐기로 끌고 감 (EMA 역산으로 증명)
run10~11 연속성 가드(0.10/0.15) 실험 → 0.10 확정, 회전력 부족은 별개 문제로 분리
run12    ★ 사상 첫 자율 분기 진입 (1L 모드) → RA 정지선 검출까지. ra_min_drive 10s 가
         진짜 진입을 0.7초 차로 차단 → 7.5s 로 완화
run13    ★ 사상 첫 자율 ROUNDABOUT 진입 + 진입 락온 + 링 2초 추종 → 섬 쪽 소실
run14    1L 조기무장 결함 발견 (래치 순간 무장 → 안쪽 실선을 바깥선으로 오인, 코너 깎음)
         → 발동을 '안쪽 실선 소실 순간'으로 이동
run15    ★ 역대 최상 진입 (완만→깊게→수렴) + RA 진입 3연속. 링에서 언더스티어(사용자
         육안) → circle_steer_bias 0 이 진입 총 편향까지 깎은 것으로 진단
run16    폐기 (배터리 8.07V 에서 스로틀 부족, 출발부터 부진 → 인코스 진입 전 정지)
```

## 0.5. 오늘 오후 배포된 변경 (전부 보드 반영+빌드 확인)

| 항목 | 값/내용 | 근거 |
|---|---|---|
| `follow_yellow_ratio` | 0.01 실험 후 **0.03 복원** | run8: 0.01 은 방황 중 3% 노란 조각 재래치로 우측 이탈 유발. 램프가 0.3초에 0.01→0.05 통과 → 문턱은 죽은 레버 |
| `yellow_slow_ratio` | **0.03** (추종 문턱과 동기) | 감속과 래치가 같은 순간 걸리게 |
| `center_jump_max_ratio` | **0.10 (신규)** — 최종 차선중심 프레임당 이동 ≤ 반차폭×0.10. **nl=2 면 스킵** | run9 쐐기 점프 동결 실증. 0.15 는 쐐기 당김(0.05~0.08/frame)이 통과해 실패 (run11) |
| **진입 병합(1L) 모드 (신규)** | `entry_oneline_frames=40`: 안쪽 실선 소실 순간부터 2초, 밴드 좌/우 분할 OFF, 노란 전체평균 = 바깥선 하나, 중심 = 선−반차폭. 래치당 1회, RA 후 재무장 없음 | run9~11 쐐기 원천 차단. run14 로 "래치 순간 무장"의 코너 깎기 결함 확인 → 소실 순간으로 이동 (run15 검증) |
| `ra_min_drive_s` | 10.0 → **7.5** | run12: 진짜 정지선 도착 9.3s < 10 으로 차단당함. 가짜 5.9s +1.6 여유 / 진짜 9.3s -1.8 여유 |
| `circle_steer_bias` | -0.13 → **0.0** (링 PID 단독 시험 중) | run13: 링 추종 중 steer=0.26+0.13 상시 → 섬 진입. 단 run15 에서 진입 총 편향까지 깎인 부작용 확인 |
| `entry_steer_bias` | -0.15 → **-0.22** | run15 언더스티어 (진입 락 steer 0.41~0.53, run13 은 0.58~1.0). 사용자 지정값 |

## 0.6. 오늘 오후의 사고/교훈

- **i2c-3 버스 잠김 재발 (run15 정지 직후)** — 빈 주소까지 errno=16. **이 상태에선 중립
  PWM 을 쓸 수 없어 차가 소프트웨어로 안 멈춘다** (PWM 칩이 마지막 스로틀 유지, ESC 전원이라
  보드를 꺼도 계속). **비상정지 = ESC 스위치 OFF 뿐.** 복구는 늘 그렇듯 전원 완전 차단 10초.
- **텔레메트리 좀비**: ssh 스트림 방식은 노트북 쪽 ssh 를 죽여도 보드 쪽 python 이 살아남는다.
  런치 8회 만에 좀비 8개 누적 → stop.sh 가 안 잡음. 재부팅으로 클리어됨. 다음 세션은
  telemetry 실행 전 `pkill -TERM -f telemetry_dual` 습관화할 것.
- **배터리-스로틀 결합 재확인**: 8.07V(잔량 ~70%)에서 kick 0.22 로 출발 부진/정지 (run16).
  완충(8.3V)에서는 전 런 정상. §5 의 "보수적 값" 원칙 아직 미적용 — kick 0.25 승격 논의 중.

## 0.7. 현재 미션 체인 상태 (오후 기준)

```
초록불 → 흰 추종     ○ 안정
분기(흰→노랑) 진입    ○ 1L 로 3/4 성공 (run12,13,15. run14 는 조기무장 결함, 수정됨)
노란 접근로 추종      ○ 안정 (yr 0.05~0.09)
RA 게이트(정지선+시간) ○ 3연속 (7.5s 가드)
RA 진입 락온          △ 언더스티어 → entry -0.22 로 증강, 검증 대기
링 순환              ✕ 최장 2초 (PID 단독은 미검증 — 다음 런이 첫 시험)
링 탈출~이후          미도달
```

---

# 인수인계 — 2026-07-11 (아침)

이전 인수인계(아래 07-10 밤 섹션) 이후 밤샘 세션의 변경분.

## 0. 지금 상태 (07-11 오전 갱신)

- 보드는 주간 팀원의 "iPhone" 핫스팟에 붙음: **172.20.10.6** (`~/.ssh/config` d-racer 갱신됨).
  보드 MAC = `fc:22:1c:40:62:96`. 옛 핫스팟 프로필(임태후/우스구시/luckyderek)은
  autoconnect off 처리 (멋대로 옮겨가는 것 방지).
- 오전에 와이파이 동글이 USB 에서 통째로 사라짐 (재삽입 2회 무효) → 정상 종료
  (`sudo poweroff`) 후 전원 재인가로 복구. e2fsck USB 스틱은 여전히 lsusb 에 안 보임 —
  허브에서 빠진 듯, 복구용이니 찾아서 다시 꽂아둘 것.
- **밤새 배포 대기였던 수정 2개, 07-11 오전 배포+빌드 완료** (보드 install 에서 값 확인):
  1. `ra_min_drive_s = 10` — 출발 10초 내 RA 진입 금지 (인코스 '입구'에서 노란 차선을
     정지선으로 오인해 5.9초 만에 오진입한 것 차단)
  2. `entry_steer_bias = -0.15` — RA 진입 락온 첫 ~2초간 좌조향 피드포워드
     (3개 런 연속 진입 갈림길 언더스티어 → 링 선 놓침 대응)
- `gate_blank_s / min_loop_time_s = 17` 도 소스 기본값으로 승격 (라이브 주입 불필요해짐).
- **아직 실주행 검증 전** — 다음 자율 런에서 ①입구 오진입 차단 ②진입 언더스티어 완화를 확인할 것.

## 1. 자율주행 진행도 (오늘 아침 기준)

```
초록불 인식      ○  (안정)
흰 구간 추종     ○  (off ±0.1)
노란 접근 추종   ○  (13초 안정 구간 확인)
RA 진입 판정     ○  (3연속 성공. 입구 오진입 1회 → ra_min_drive_s 배포됨, 검증 대기)
RA 진입 직후     ✕  (급좌회전 언더스티어 → 매 런 개입 1회. entry_steer_bias 배포됨, 검증 대기)
링 순환          △  (21초 체류 성공 1회, 요동 있음)
가짜 정지선 무시  ○  (yaw_gate_min 4.2 가 실전에서 차단 확인 — 오늘 최대 성과)
탈출 게이트      △  (정상 위치 탈출 1회 확인)
탈출 후 복귀     ✕  (탈출로 갈아타며 차선 소실 → 우측 표류)
빨간도로/아루코   미검증 (트랙에 구간 없음 / aruco_node 는 OpenCV 5 버그로 죽음)
```

## 2. 이번 세션에서 찾아 고친 것 (원인 → 수정)

| 문제 | 원인 (실측) | 수정 |
|---|---|---|
| 출발 직후 이탈 | yr 0.005 문턱이 원거리 노란 마킹에 조기 래치 | `follow_yellow_ratio` 0.03 복원 |
| nl=2 에서도 off ±0.9 요동 | 헤딩 보정의 a_h(전체 피팅)가 ±2.5 널뜀 × gain 0.4 | `yellow_heading_gain = 0` |
| 단선 좌/우 오분류 요동 | 화면 반쪽 기준 분류가 프레임마다 뒤집힘 | 직전 중심 연속성 분류 추가 |
| **링에서 항상 바깥 이탈** | **`circle_steer_bias` 부호 반전** — steer_scale 을 -1 로 뒤집을 때 같이 안 뒤집힘. 편향이 링 바깥(우)으로 밀고 있었음 | 부호 반전 (음수 = 안쪽). 크기 -0.13 |
| yaw_proxy 영영 0 | 같은 부호 문제 | defl 부호 반전, `yaw_lap_threshold` 9.0 |
| 진입 락온 즉시 해제 | fork 1프레임 깜빡임이 단일엣지 재수렴 성립 | 0.3초 부재 + 0.5초 최소유지 디바운스 |
| 링 위 blip 으로 조기 탈출 | 탈출 게이트가 1프레임 엣지 카운트 | `gate_sustain_s = 0.25` |
| **링 중간 '진짜 실선'으로 조기 탈출** | 이미지로 구분 불가 (부호·solidity·junction 전부 실측 겹침. **진짜 탈출선도 링에서 보면 slope +0.5**) | **`yaw_gate_min = 4.2`** — 조향 적분(가짜 지점 2.6~3.2 실측)이 4.2 넘어야 게이트 무장. **auto6 에서 차단 동작 실증** |
| BEV 어긋남 | 카메라 재조정 후 재캘리 누락 (최대 36px) | `birdeye_src_ratio` 재캘리 (검증 ±0.003) |

## 3. 현재 파라미터 (소스 기본값 + 라이브 관례)

```
스로틀: drive 0.18 (흰) / yellow_drive 0.16 (노란) / slow 0.155 / kick 0.22
        ※ 완충 기준. 배터리 잔량에 따라 재검증 필요 (§07-10 참조)
RA:     circle_steer_bias -0.13, entry_steer_bias -0.15, entry_lock 2.0s
진입:   enter_sustain 0.2s, enter_max_curvature 0(off), ra_min_drive 10s
탈출:   exit_gates 1, gate_sustain 0.25s, yaw_gate_min 4.2,
        gate_blank/min_loop 17s(07-11 소스 기본값으로 승격), max_loop 30s
        ※ 실측 랩: 손 19.7초 (1개뿐), 자율 완주 랩 ~21초 추정 1회
```

17초는 자율 랩타임 실측이 쌓이면 재확정할 것 (진짜 탈출 ~20초+ 대비 여유 3초뿐).

## 4. 운영 요령 (이 세션에서 확립)

- **"멈춰" 프로토콜**: 사용자가 멈추라 하면 다른 것 전에 `bash /tmp/stop.sh` 먼저.
- **개입 기록**: 주행 중 손/발로 개입했으면 몇 초쯤인지 기록 → 그 구간은 분석에서 제외.
  개입 구간 서명: 조향은 큰데 offset 이 안 따라오거나 순간 점프.
- **텔레메트리**: `python3 /tmp/telemetry_dual.py` → `~/telemetry.jsonl` (fsync, 재부팅 생존).
  분석: 전이 이벤트 먼저 → 의심 구간 0.2~0.4초 간격 프레임 → 사용자 관찰과 대조.
- **재부팅 후**: `/tmp` 스크립트 전부 소실 → scratchpad 에서 재배포 필요
  (launch.sh stop.sh telemetry_dual.py). 소스 기본값은 유지됨.
- Wi-Fi 죽으면: picocom 에서 nmcli connect, 안 되면 동글 물리 재삽입, 최후 reboot.
  `wifi-recover.service` (USB rebind 워치독) 설치되어 있으나 완전하지 않음.
- 보드 수수께끼 재부팅 (`bootreason=normal` + UNCLEAN) 이 세션 중 5회+. 원인 미상.
  fsync 텔레메트리 덕에 데이터 손실은 없음.

## 5. 다음 할 일 (우선순위)

1. ~~배포 대기 2건 배포+빌드~~ (07-11 오전 완료) → 자율 재시도
2. RA 진입 직후 안정화 검증 (entry_steer_bias 효과 — 유일하게 매 런 개입하던 지점)
3. 탈출 후 차선 복귀 (탈출로 갈아타기) — 아직 손 안 댐
4. 자율 랩타임 확정 → gate_blank/min_loop/yaw_gate_min 재캘리
5. aruco_node IndexError (OpenCV 5 API) — 아루코 미션 자체가 죽어 있음
6. FINISH(빨간불 정지) 경로 실전 검증


